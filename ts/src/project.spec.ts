/// <reference path="../typings/mocha/mocha.d.ts" />
import Project from './project';

describe('Project', () => {

  it('should create', () => {
    let p = new Project('abc');
    throw new Error('not working!');
  });
});
